<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header">Lista de Enfermedades Especificas</h1>
    </div>
    <%= link_to new_quantity_path(@sickness_specific), :class => 'btn btn-success' do %>

      <font color="White">    Nueva Enfermedad Especifica </font>
    <% end %>
    <p id="notice"><%= notice %></p>
    <!-- /.col-lg-12 -->
</div>
<!-- /.row -->
<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                DataTables Advanced Tables
            </div>

            <!-- /.panel-heading -->
            <div class="panel-body">
                <table width="100%" class="table table-striped table-bordered table-hover" id="dataTables-example">

  <thead>
    <tr>
      <th>Sex</th>
      <th>State</th>
      <th>Sickness specific</th>
      <th>Period</th>
      <th>Age</th>
      <th>Cantidad</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @quantities.each do |quantity| %>
      <tr>
        <td><%= quantity.sex.name %></td>
        <td><%= quantity.state.name %></td>
        <td><%= quantity.sickness_specific.name %></td>
        <td><%= quantity.period.year %></td>
        <td><%= quantity.age.edad %></td>
        <td><%= quantity.cantidad %></td>
        <td><%= link_to 'Mostrar', quantity, :class => 'btn btn-primary' %></td>
        <td><%= link_to 'Editar', edit_quantity_path(quantity), :class => 'btn btn-warning' %></td>
        <td><%= link_to 'Eliminar', quantity, method: :delete, data: { confirm: '¿Seguro desea Eliminar?' }, :class => 'btn btn-danger' %></td>


      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Quantity', new_quantity_path %>
