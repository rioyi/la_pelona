<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header">Lista Enfermedades General</h1>
    </div>
    <%= link_to new_sickness_general_path(@sickness_general), :class => 'btn btn-success' do %>

      <font color="White">    Nueva Enferemedad General </font>
    <% end %>
    <p id="notice"><%= notice %></p>
    <!-- /.col-lg-12 -->
</div>
<!-- /.row -->
<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                DataTables Advanced Tables
            </div>

            <!-- /.panel-heading -->
            <div class="panel-body">
                <table width="100%" class="table table-striped table-bordered table-hover" id="dataTables-example">

  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @sickness_generals.each do |sickness_general| %>
      <tr>
        <td><%= sickness_general.name %></td>
        <td><%= link_to 'Mostrar', sickness_general, :class => 'btn btn-primary' %></td>
        <td><%= link_to 'Editar', edit_sickness_general_path(sickness_general), :class => 'btn btn-warning' %></td>
        <td><%= link_to 'Eliminar', sickness_general, method: :delete, data: { confirm: '¿Seguro desea Eliminar?' }, :class => 'btn btn-danger' %></td>


      </tr>
    <% end %>
  </tbody>
</table>

<br>
