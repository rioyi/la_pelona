            <div class="row">
                <div class="col-lg-12">
                    <h1 class="page-header">Lista de Estados</h1>
                </div>
                <%= link_to new_state_path(@state), :class => 'btn btn-success' do %>
                  <font color="White">    Nuevo Estado </font>
                <% end %>
                <p id="notice"><%= notice %></p>
                <!-- /.col-lg-12 -->
            </div>
            <!-- /.row -->
            <div class="row">
                <div class="col-lg-12">
                    <div class="panel panel-default">
                        <div class="panel-heading">
                            DataTables Advanced Tables
                        </div>

                        <!-- /.panel-heading -->
                        <div class="panel-body">
                            <table width="100%" class="table table-striped table-bordered table-hover" id="dataTables-example">
                                <thead>
                                    <tr>
                                      <th colspan="10">Name</th>
                                      <th></th>
                                    </tr>
                                </thead>
                                <tbody>
                                  <% @states.each do |state| %>

                                  <td colspan="10"><h5><%= state.name %></h5></td>
                                  <font color="White">
                                  <td><%= link_to 'Mostrar', state, :class => 'btn btn-primary' %></td>
                                  <td><%= link_to 'Editar', edit_state_path(state), :class => 'btn btn-warning' %></td>
                                  <td><%= link_to 'Eliminar', state, method: :delete, data: { confirm: '¿Seguro desea Eliminar?' }, :class => 'btn btn-danger' %></td>
                                  </font>
                                </tbody>
                                <% end %>
                            </table>

<!--
                            <table>
                              <thead>
                                <tr>
                                  <th>Name</th>
                                  <th colspan="3"></th>
                                </tr>
                              </thead>

                              <tbody>
                                <% @states.each do |state| %>
                                  <tr>
                                    <td><%= state.id %></td>
                                    <td><%= state.name %></td>
                                    <td><%= link_to 'Show', state %></td>
                                    <td><%= link_to 'Edit', edit_state_path(state) %></td>
                                    <td><%= link_to 'Destroy', state, method: :delete, data: { confirm: 'Are you sure?' } %></td>
                                  </tr>
                                <% end %>
                              </tbody>
                            </table>

-->
